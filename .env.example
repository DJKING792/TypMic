# 小米 MiMo ASR API key（前往 https://platform.xiaomimimo.com 申请）
# 用法：把本文件复制为同目录的 .env，并填入你的 key，然后重启服务即可。
MIMO_API_KEY=在这里填写你的key

# —— 可选：识别引擎（默认云端 MiMo）——
# TYPOMIC_ASR 取值：mimo（默认，云端小米 MiMo）/ whisper（本地 faster-whisper）/ sensevoice（本地 SenseVoice）
# 兼容旧值：cloud 等同 mimo，local 等同 whisper。
# 离线引擎（whisper / sensevoice）语音不出本机，无需任何 API key，但需先装对应依赖并首次下载模型。
#   whisper   ：pip install -r requirements-offline.txt
#   sensevoice：pip install funasr modelscope
# TYPOMIC_ASR=mimo

# —— 可选：本地 faster-whisper（TYPOMIC_ASR=whisper 时生效）——
# WHISPER_MODEL=small        # 可选：tiny/base/small/medium/large-v3（越大越准越慢；注意 Whisper 中文识别弱于 MiMo，英文更佳）

# —— 可选：本地 SenseVoice（TYPOMIC_ASR=sensevoice 时生效）——
# SENSEVOICE_MODEL=iic/SenseVoiceSmall   # ModelScope 模型名（默认即可）
# SENSEVOICE_DEVICE=cpu                   # 推理设备：cpu 或 cuda:0（有 NVIDIA 显卡填 cuda:0 更快）

# —— 可选：AI 润色（默认关闭，设为 on 开启）——
# 开启后，识别出的原文会先过「术语表纠正」，再送 LLM 整理。重点做三件事：
#   1) 自动补全中文标点（逗号/句号/顿号等）——口述文本能否直接用的关键；
#   2) 解释命令词：说「新段落/换行」=分段、「逗号/句号/问号/感叹号/冒号/分号」=对应标点；
#   3) 轻度去口头禅、顺句、修正同音口误、长文分段、数字金额规范。
# 润色是增益：接口异常/无 key 会自动降级为不润色，不影响正常输入。
# TYPOMIC_POLISH=on
# 润色接口（OpenAI 兼容 chat/completions），默认复用小米 MiMo 对话端点
# TYPOMIC_POLISH_URL=https://api.xiaomimimo.com/v1/chat/completions
# 润色模型名（需为支持 chat 的模型）。注意：api.xiaomimimo.com 的 chat 端点
# 实测【不支持 mimo-v2.5-flash】（返回 400 Unsupported model），不要填 flash 系列；
# 推荐 mimo-v2.5（快且稳）。mimo-v2.5-pro 可用但偏慢、易触发超时，按需使用。
# TYPOMIC_POLISH_MODEL=mimo-v2.5
# 润色 API key（默认复用上面的 MIMO_API_KEY；如需用别的账号可单独填）
# TYPOMIC_POLISH_API_KEY=
# 润色模式（桌面页也可运行时随时切换）：
#   full       通用润色（去废话+顺句+分段+标点，默认风格）
#   logic      理顺逻辑（适合说话散乱：补连接、理清层次）
#   novel      小说创作（保留文学性/口语感，不强行规范化）
#   business   公司文案（商务正式、专业、结构清晰）
#   admin      行政公文（公文规范、条理、庄重）
# TYPOMIC_POLISH_MODE=full

# —— 可选：术语表（错词纠正）——
# 把项目里的 glossary.txt.example 复制为 glossary.txt 并按需修改即可生效（重启加载）。
# 无需任何 key，纯本地文本替换。
# 也可用 TYPOMIC_GLOSSARY=off 彻底关闭术语表（默认 on）。
# TYPOMIC_GLOSSARY=off

