# 小米 MiMo ASR API key（前往 https://platform.xiaomimimo.com 申请）
# 用法：把本文件复制为同目录的 .env，并填入你的 key，然后重启服务即可。
MIMO_API_KEY=在这里填写你的key

# —— 可选：离线模式（本地 faster-whisper 识别，语音不出本机，无需任何 API key）——
# 取消下面两行注释即可启用离线模式（需先：pip install -r requirements-offline.txt）
# TYPOMIC_ASR=local
# WHISPER_MODEL=small        # 可选：tiny/base/small/medium/large-v3（越大越准越慢；注意 Whisper 中文识别弱于 MiMo，英文更佳）

# —— 可选：AI 润色（默认关闭，设为 on 开启）——
# 开启后，识别出的原文会先过「术语表纠正」，再送 LLM 整理。重点做三件事：
#   1) 自动补全中文标点（逗号/句号/顿号等）——口述文本能否直接用的关键；
#   2) 解释命令词：说「新段落/换行」=分段、「逗号/句号/问号/感叹号/冒号/分号」=对应标点；
#   3) 轻度去口头禅、顺句、修正同音口误、长文分段、数字金额规范。
# 润色是增益：接口异常/无 key 会自动降级为不润色，不影响正常输入。
# TYPOMIC_POLISH=on
# 润色接口（OpenAI 兼容 chat/completions），默认复用小米 MiMo 对话端点
# TYPOMIC_POLISH_URL=https://api.xiaomimimo.com/v1/chat/completions
# 润色模型名（需为支持 chat 的模型，按你的账号调整）
# TYPOMIC_POLISH_MODEL=mimo-v2.5-flash
# 润色 API key（默认复用上面的 MIMO_API_KEY；如需用别的账号可单独填）
# TYPOMIC_POLISH_API_KEY=

# —— 可选：术语表（错词纠正）——
# 把项目里的 glossary.txt.example 复制为 glossary.txt 并按需修改即可生效（重启加载）。
# 无需任何 key，纯本地文本替换。

